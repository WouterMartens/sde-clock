public class Console {
    /**
     * Write a given message to the console
     * @param message Message to write to the console
     */
    public void write(String message) {
        System.out.println(message);
    }
}
